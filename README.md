# ToDo App – Spring Boot Backend

This is the backend of a simple To-Do list application using:

- Java Spring Boot
- MySQL
- Spring Web
- Spring Data JPA
- CORS config

## Endpoints

- `GET /api/todos`
- `POST /api/todos`
- `PUT /api/todos/{id}`
- `DELETE /api/todos/{id}`

## Setup

```bash
mvn spring-boot:run
